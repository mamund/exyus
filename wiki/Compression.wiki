#summary Describes Compression services
#labels Phase-Implementation

Exyus offers automatic support for gzip and deflate compression via a pipeline HTTPModule. This is turned on by default, but can be turned off by commenting out the module reference in the [WebConfig Web.config] file at the site root.